# /bijiao 功能交接文档

更新时间：2026-07-16

## 1. 业务目标与当前状态

`/bijiao` 用于比较两份投标 PDF，识别有调查价值的雷同内容，并可选上传一份招标文件，用于剔除投标文件共同照抄的招标原文。当前代码已部署到腾讯云，主分支版本为 `dfbf856`；最近与比对功能直接相关的提交为：

- `2f2bafb`：提高投标文件比对准确性，补充前端与测试。
- `628f0f1`：优化准确性与资源占用，并扩大 PDF 页数能力。
- `09ee336`：进一步过滤无价值结果。
- `0f92c5d`：保留参数表中的实质性共同新增内容，避免规则过严。

用户的核心质量要求：结果必须有审查价值。不要为了召回率把模板、招标原文、页眉页脚或泛化的标点/编号差异重新呈现出来；但也不能把规则收得过紧，以致真正的共同雷同或共同修改被漏掉。

`rokid_glasses_app` **不调用** `/api/compare`。不过该接口已在本项目使用，仍应保持请求和响应兼容，避免无必要的字段删除或改名。

## 2. 入口、接口与文件位置

| 位置 | 作用 |
| --- | --- |
| `dashboard/templates/bijiao.html` | 上传界面、结果筛选、CSV 导出、打印报告和前端渲染。 |
| `dashboard/app.py` | `/bijiao` 页面、`/api/compare` 上传处理、归档与错误响应。 |
| `dashboard/utils/comparator.py` | PDF 文本提取、缓存、候选检索、雷同/共同错误/招标关联判断。 |
| `tests/test_comparator.py` | 48 个核心回归测试。 |
| `data/bijiao_cache/` | PDF 提取结果缓存；受 256 MB 上限控制，超量按访问时间清理。 |

接口：`POST /api/compare`

- 必填 multipart 文件：`file_a`、`file_b`。
- 可选 multipart 文件：`file_tender`，作为招标原文排除依据。
- 仅接受 PDF，重复内容（归档 MD5 一致）会被拒绝。
- 可选开关：`check_entity`、`check_text`、`check_spelling`，前端默认都启用。
- 成功响应：`{ status: "success", data: { metadata, summary, paragraphs } }`。
- 失败时以 400 返回输入、PDF 或资源限制问题；其他异常返回 500。
- 上传原文件会先归档，临时上传文件在请求结束时删除；归档文件用于后续缓存复用。

页面当前分类和筛选项：完全雷同、近似雷同、已验证招标共同修改、共同错误、敏感实体。不要仅改后端分类名称而不同时检查前端筛选、指标统计、CSV 导出和打印报告。

## 3. 当前算法要点

### PDF 提取和资源边界

- 使用 PyMuPDF (`fitz`) 按页、按阅读顺序提取文本；会记录页码、文档元数据和疑似扫描页统计。
- 文本先做 NFKC、常见中英文标点统一、空白移除和小写化；数字不会被简单删除。
- 资源限制见 `dashboard/utils/comparator.py` 文件头：单文件最多 2,000 页、单次合计最多 4,000 页；单文件可提取文本最多 8,000,000 字符、单次最多 12,000,000 字符。
- 还限制精确/模糊单元数量、候选数和每个 shingle 的倒排数量，避免 2 核 2GB 服务器被大文档或公共模板拖垮。
- 这些上限是用户要求“尽量扩大最大 PDF 页数”后在小规格服务器上设置的折中。提高上限前，必须使用接近上限的真实 PDF 观察内存、CPU、超时和容器重启风险。

### 雷同检测

- 精确雷同：以页内文本单元和连续片段为基础，合并相邻、确实连续的完全匹配片段，避免同一段被拆成多条；不得跨无关内容或不同章节顺序强行合并。
- 近似雷同：使用固定长度 shingle 的紧凑签名建立候选，再做受限的 SequenceMatcher 比对；精确匹配已覆盖的近似结果会抑制，单一目标单元不能被无限重复复用。
- 会抑制常见投标表单、资质/业绩材料大纲、页码计数行等低价值模板文本。
- 页面统计会提示疑似扫描页；当前功能不做 OCR。扫描件文字少时，应在结果解释中提示而非把“无结果”理解为“无雷同”。

### 招标文件排除和“共同修改”

- 上传招标文件后，投标 A/B 中来自招标原文的内容应被排除，不能因为共同照抄而当作投标雷同。
- 只有 A/B 都呈现可验证、相同的招标文本改动时，才可归类为“已验证招标共同修改”。一份文件单独修改、不同改动、仅表格单元重排、或只有短小碎片时应抑制。
- 参数表是重点边界：若两份投标文件存在实质性、共同的非招标新增内容，应保留；不能因表格整体与招标相似就一律过滤。相关回归测试为 `test_parameter_table_keeps_substantial_shared_nontender_content`。
- 用户曾明确认为早期“招标原文近似共同修改线索”参考价值不大。后续调整不能恢复宽泛的“近似即共同修改”逻辑。

### 共同错误

当前仅从相对可靠、可解释的角度报告，不做泛化拼写纠错：

- 同一上下文中的连续异常标点、未闭合括号等。
- 编号后双重分隔符、连续编号中的重复号、同一编号列表中明显的格式离群项。
- 两份文件中相同、可明确计算的算术错误。
- 招标文件本身已有的错误不应报告为投标文件“共同错误”；不同上下文的相同标点模式、正常编号括号、仅编号跳号、合法计算均应抑制。

## 4. 用户反馈与调优原则

1. 最初问题：结果混乱；“招标原文近似共同修改”不准确；同一完全匹配内容被拆成多个段落。
2. 已做：连续精确片段合并、招标来源验证、近似结果抑制、共同错误的上下文和规则约束。
3. 后续问题：页面仍有大量无价值结果，因此规则曾进一步收紧。
4. 用户随后指出规则可能又过紧，并要求先人工分析样本中真正雷同的内容，再让代码结果向人工判断靠近。

后续任何阈值或过滤调整都应遵循此顺序：

1. 先人工通读招标文件与两份投标文件，列出真正有价值的共同片段及应排除的原因。
2. 用当前版本生成结果，逐条比较漏报、误报和重复展示。
3. 只针对明确模式调整规则，并添加最小回归测试；不要用单一全局阈值粗暴扩大或缩小结果集。
4. 用测试和真实三文件样本共同验证，重点检查结果数量、类型分布、片段是否合并、页码是否正确及招标内容是否被误报。

## 5. 真实回归样本

用户曾用以下目录中的 3 份文件做浏览器验证：

`C:\Users\ai-lilac\Desktop\file`

其中招标文件名称以 `F19山西杏花村汾酒厂股份有限公司2030项目各单位办公家具采购6.16` 开头，另外两份为投标文件。下一会话应优先沿用这组文件：

- 先人工确认应保留的雷同、共同改动、共同编号/标点问题。
- 再访问 `http://82.157.244.174/bijiao` 复现当前输出。
- 不要上传或公开这些源文件到 GitHub/Gitee。

## 6. 已验证测试

2026-07-16 本地执行：

```powershell
.\venv\Scripts\python.exe -m unittest tests.test_comparator -v
```

结果：48 项全部通过。测试覆盖精确片段合并、近似候选去重、招标来源/共同修改、参数表边界、标点/编号/计算错误、PDF 页数与字符限制、缓存和扫描页统计。

代码修改后至少再次运行该命令；涉及真实结果质量时，还必须运行上述三份 PDF 的人工回归，单元测试不能替代人工判断。

## 7. 部署与仓库约束

- 服务器为腾讯云 2 核 2GB：不要引入大模型、OCR 常驻服务、重型全文搜索或无上限并行任务。
- 当前生产地址为 `http://82.157.244.174/bijiao`，容器为 `bidding-app`。
- 代码提交前须排除所有 `.md` 文件；本交接文件也不提交。
- 完成代码任务后，先询问用户是否提交并推送 GitHub 和 Gitee；用户确认后再询问是否部署到腾讯云。
- 若对“哪些结果有业务价值”存在疑问，先向用户提问，不要自行改变判定口径。

## 8. 建议的新会话起始任务

“请以 `BIJIAO_HANDOVER.md` 为背景，使用 `C:\Users\ai-lilac\Desktop\file` 中的招标文件和两份投标文件，先人工分析真正有价值的雷同和应过滤的内容；再对照当前 `/bijiao` 的输出，列出误报、漏报、重复展示及其对应代码位置。先给出结论和最小改进方案，未经确认不要修改。”
