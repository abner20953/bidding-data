# AGENTS.md

## AI 协作入口（必须读取）

- 处理工作台 `/pingbiao`、`/api/evaluation-workbench` 或 `dashboard/evaluation_workbench/` 相关任务时，必须先阅读 `docs/ai协作/AI_CONTEXT.md`，再按其中的任务路由读取稳定设计与当前交接。
- 工作台的 AI 协作文档统一位于 `docs/ai协作/` 并由 Git 管理。除本文件因开发工具自动发现必须保留在根目录外，不得在其他目录复制或另建内容相同的上下文、设计、记忆或交接文件。
- `AI_HANDOFF.md` 只记录动态状态，必须先用 Git、测试和云端运行时版本核对；不得把其中的过期状态当成当前事实。

## 项目背景

- 本项目部署在腾讯云 2 核 2 GB 服务器上。所有开发、依赖选择、后台任务、模型加载、Docker 构建和运行时行为，都必须考虑这个小规格硬件环境。
- 优先采用低内存、CPU 友好的实现。不要随意引入 GPU 专用依赖、大型常驻模型、重型前端框架或高内存常驻后台任务，除非用户明确认可这个取舍。
- 腾讯云部署路径围绕 `Dockerfile.tencent`、CPU 版 PyTorch、国内镜像源和项目内置 `redeploy.sh` 脚本优化。
- `/dlsgzs` 是评标专家库模块，包含专家档案、身份证照片上传、腾讯云人脸库同步、人脸照片比对检索和实时视频人脸识别签到。
- 当前项目有部分 API 会被另一个项目 `rokid_glasses_app` 调用，用于提供部分功能支持。修改任何现有 API 路径、请求参数、响应字段、状态码或认证/跨域行为前，都要考虑对 `rokid_glasses_app` 的兼容性，避免无提示破坏外部调用方。

## 工程原则（基础要求）

- 治本优先：可靠性必须构建在问题产生的环节内部（提取、评审、落库），而不是在流程之外堆检测器、恢复器或兜底 UI。新增机制前自检“如果上游正常，这个机制是否还需要”；确需兜底时，必须按“校验 → 定向重试 → 显式标记 → 最后兜底”的顺序，禁止静默代劳。
- 通用性：所有修改必须是通用机制，禁止针对单一项目、单一投标人、单一案例或具体文本打补丁；禁止写死项目名、设备名、金额、页码。行业词表只允许法律/政策层面的硬性清单，且不得随项目特例膨胀。
- 确定性优先：能用本地确定性校验、结构化锚点（条款 ID、页码、采购清单、分值台账）就不依赖模型归纳；模型输出必须受输出契约约束，违反契约走定向重试，而不是用正则猜测兜底。
- 简洁可维护：不为修补而增加功能；每个新字段、新接口、新按钮都要有明确且唯一的目的；移除功能要干净（同步删除接口、前端引用与测试）。
- 数据兼容：新增字段/接口向后兼容，不删除旧字段；存量数据清理走可逆操作（停用而非删除、标记而非静默改写）。

## API 兼容约定

- 对已存在的 `/api/...` 和 blueprint API，优先保持向后兼容。
- 如必须调整接口行为，优先新增字段或新增版本化接口，不要直接删除旧字段或改变旧字段含义。
- 涉及 `rokid_glasses_app` 可能调用的接口时，完成修改后应额外提醒用户验证该外部项目。

## 业务流程决策

- 如果对业务流程、业务规则、数据口径、审批/部署流程、专家库逻辑、采集筛选逻辑或外部项目调用关系存在疑问，必须先向用户提问确认。
- 不要基于猜测或自以为合理的判断轻易修改业务流程。
- 可以自行处理明确的代码错误、样式问题、性能问题和安全问题；但如果修复会改变用户工作流或业务含义，应先说明影响并等待用户确认。

## 参考文档

- 腾讯云人脸识别 API 文档：人脸搜索分库返回
  https://cloud.tencent.com/document/product/867/44993

## 仓库流程

- 本项目代码同时保存在 GitHub 和 Gitee。已配置的 remote 名称分别是 `github` 和 `gitee`。
- 每次代码任务完成并验证后，询问用户是否提交并推送到这两个远程仓库。
- 未经用户确认，不要自动提交或推送代码。

## 部署流程

- 代码经用户确认提交并推送后，询问用户是否部署到腾讯云服务器。
- 腾讯云 SSH 目标是 `root@82.157.244.174`。
- 服务器上的项目目录默认是 `~/bidding-data`。
- 首选服务器部署命令：

```bash
cd ~/bidding-data
chmod +x redeploy.sh
./redeploy.sh
```

- `redeploy.sh` 会执行 `git pull`、使用 `Dockerfile.tencent` 重新构建镜像、重建 `bidding-app` 容器、挂载持久化目录，并跟踪容器日志。

## 安全规则

- 不要把服务器密码、API Key、Token、Cookie 或其他密钥写入仓库、`AGENTS.md`、日志、命令或最终回复。
- SSH 部署应使用已配置的 SSH key、SSH agent、本地 SSH config，或向用户请求明确的运行时授权。
- 如果确实需要临时密码认证，可以从运行环境变量 `SSH_PASSWORD` 读取；不要打印变量值，不要写入文件，也不要提交到远程仓库。
- 如果密码或其他密钥出现在对话中，不要持久化保存；如果它可能是真实生产凭据，应建议用户轮换。
