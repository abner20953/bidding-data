diff --git a/dashboard/static/evaluation_workbench/app.js b/dashboard/static/evaluation_workbench/app.js
index c75474f..7da6ec3 100644
--- a/dashboard/static/evaluation_workbench/app.js
+++ b/dashboard/static/evaluation_workbench/app.js
@@ -191,6 +191,62 @@
     $('model-profiles').querySelectorAll('.delete-model').forEach((button) => button.onclick = async () => { if (!confirm('删除模型档案后，所有项目将无法再选择该模型；历史结果会保留。是否继续？')) return; try { await request(`/model-profiles/${button.dataset.profile}`, {method:'DELETE'}); if ($('model-profile-id').value === button.dataset.profile) resetModelForm(); await loadProfiles(); } catch (error) { alert(error.message); } });
   }
   function categoryLabel(value) { return {qualification:'资格性', compliance:'符合性', substantive:'实质性/废标项', rejection:'实质性/废标项', other:'其他规则', objective:'客观分', subjective:'主观分'}[value] || value; }
+  const acquisitionPresetLabels = {smart:'智能取证', always:'每次执行', text:'扫描文字核验', visual:'签章/外观核验', dual:'双重核验', off:'不进行图片取证', custom:'自定义高级设置'};
+  const acquisitionLevelLabels = {off:'关闭', low:'快速', standard:'标准', high:'充分'};
+  function inferredAcquisitionPreset(rule) {
+    if (acquisitionPresetLabels[rule?.acquisition_preset]) return rule.acquisition_preset;
+    return ({ocr_only:'text', vision_only:'visual', combined:'dual', off:'off'})[rule?.image_mode] || 'smart';
+  }
+  function simpleAcquisitionMode(rule) {
+    const mode = String(rule?.image_mode || 'auto'); const trigger = String(rule?.vision_trigger || 'off'); const level = String(rule?.vision_level || 'off');
+    if (mode === 'off' || trigger === 'off' || level === 'off') return 'off';
+    const preset = inferredAcquisitionPreset(rule);
+    if (['always', 'visual', 'dual'].includes(preset) || (preset === 'custom' && mode === 'auto' && trigger === 'required')) return 'always';
+    if (['smart', 'text'].includes(preset) || (mode === 'auto' && trigger === 'text_fallback')) return 'smart';
+    return 'custom';
+  }
+  function acquisitionSummary(rule) {
+    const level = String(rule?.vision_level || 'off'); const choice = simpleAcquisitionMode(rule);
+    if (choice === 'off') return {label:'未启用图片取证', state:'off', detail:'本规则只使用全文文字审查'};
+    if (choice === 'custom') return {label:`专家自定义 · ${acquisitionLevelLabels[level] || '标准'}强度`, state:'warning', detail:'已由专家模式单独指定通道或启动方式'};
+    return {label:`${acquisitionPresetLabels[choice]} · ${acquisitionLevelLabels[level] || '标准'}强度`, state:'active', detail:choice === 'always' ? '每次均执行，由系统选择必要通道' : '证据不足时按需执行'};
+  }
+  function acquisitionScene(rule) {
+    const text = `${rule?.title || ''}\n${rule?.check_rule || ''}\n${rule?.source_text || ''}`;
+    const visual = /(签字|签章|盖章|印章|骑缝章|勾选|手写|照片|版式|外观|图纸|截图|二维码)/.test(text);
+    const readable = /(证书|证件|执照|许可证|资质|报告|证明|声明函|合同|发票|表格|清单|编号|日期|有效期|型号|规格|金额|数量)/.test(text);
+    if (visual && readable) return '适用于既要核对材料文字/字段、又要看签章或外观的规则。';
+    if (visual) return '适用于签字、盖章、勾选、照片、版式、图纸或截图等外观事实。';
+    if (readable) return '适用于证书、表单、证明材料及编号、日期、金额、型号等可读字段。';
+    return '该规则通常可先由全文文字审查；仅在扫描件或图片是关键证据时启用图片取证。';
+  }
+  function acquisitionCapabilityNote() {
+    const ocr = ocrFeatureConfiguration.enabled ? 'OCR 已开启（腾讯云优先，异常时可回退本地 OCR）' : 'OCR 未开启';
+    const visionProfile = modelProfiles.find((profile) => profile.enabled && (profile.capabilities?.vision || profile.supports_vision) && profile.profile_id === visionConfiguration.default_profile_id);
+    const vision = visionConfiguration.enabled && visionProfile ? `多模态已开启（${visionProfile.display_name}）` : visionConfiguration.enabled ? '多模态已开启，但尚未选择可用图片模型' : '多模态未开启';
+    return `${ocr}；${vision}。`;
+  }
+  function acquisitionPreview(rule) {
+    const mode = String(rule?.image_mode || 'auto'); const trigger = String(rule?.vision_trigger || 'off'); const level = String(rule?.vision_level || 'off');
+    if (mode === 'off' || trigger === 'off' || level === 'off') return '当前不会启动 OCR 或多模态图片取证。';
+    const budget = {low:'最多处理少量候选页，适合单页材料或快速抽查', standard:'OCR 最多 6 页；图片首批最多 4 页、必要时可补看 4 页', high:'OCR 最多 10 页；图片首批最多 6 页、必要时可补看 6 页'}[level] || '按当前覆盖上限执行';
+    const path = {ocr_only:'仅核验扫描文字，不向模型发送图片', vision_only:'仅核验图片外观', combined:'OCR 与图片外观均会执行', auto:'由系统按材料与关键字段选择 OCR 或图片复核'}[mode] || '按规则决定取证路径';
+    return `${acquisitionScene(rule)} ${path}；${budget}。`;
+  }
+  function presetPayload(preset, level, rule = {}) {
+    const presetMap = {
+      smart:{image_mode:'auto', vision_trigger:'text_fallback'}, always:{image_mode:'auto', vision_trigger:'required'}, text:{image_mode:'ocr_only', vision_trigger:'text_fallback'},
+      visual:{image_mode:'vision_only', vision_trigger:'required'}, dual:{image_mode:'combined', vision_trigger:'required'},
+      off:{image_mode:'off', vision_trigger:'off'},
+    };
+    const mapped = presetMap[preset] || {image_mode:rule.image_mode || 'auto', vision_trigger:rule.vision_trigger || 'off'};
+    const active = preset !== 'off' && mapped.vision_trigger !== 'off';
+    return {acquisition_preset:preset, image_mode:mapped.image_mode, vision_trigger:mapped.vision_trigger, vision_level:active ? level : 'off'};
+  }
+  function simpleAcquisitionPayload(choice, level, rule = {}) {
+    if (choice === 'off') return presetPayload('off', 'off', rule);
+    return presetPayload(choice === 'always' ? 'always' : 'smart', ['low', 'standard', 'high'].includes(level) ? level : 'standard', rule);
+  }
   async function refreshRules() {
     if (!activeProject) return;
     const expandedRuleIds = new Set([...$('rules').querySelectorAll('details.rule-card[open]')]
@@ -206,16 +262,19 @@
       || String(left.title || '').localeCompare(String(right.title || ''), 'zh-CN')
     ));
     const enabledCount = data.rules.filter((r) => Boolean(r.enabled)).length; $('rule-set-meta').textContent = set ? `版本 ${set.version} · ${set.status === 'confirmed' ? '已确认' : set.status === 'draft' ? '待确认' : '已替换'} · 已启用 ${enabledCount}/${data.rules.length} 条${set.source_task_id ? ' · AI 提取结果' : ''}` : '尚未提取或添加规则。'; $('confirm-rules').disabled = !isDraft;
-    $('rules').innerHTML = displayRules.length ? `<div class="rule-card-list">${displayRules.map((r) => {
+    const acquisitionCounts = displayRules.reduce((counts, rule) => { const key = simpleAcquisitionMode(rule); counts[key] = (counts[key] || 0) + 1; return counts; }, {off:0, smart:0, always:0, custom:0});
+    const toolbar = isDraft && displayRules.length ? `<div class="rule-acquisition-toolbar"><div><strong>图片取证设置</strong><span class="muted"> 日常仅选“智能取证／每次执行”和强度，OCR、多模态通道由系统判断。</span><small>当前：不启用 ${acquisitionCounts.off || 0} 条 · 智能 ${acquisitionCounts.smart || 0} 条 · 每次执行 ${acquisitionCounts.always || 0} 条${acquisitionCounts.custom ? ` · 专家自定义 ${acquisitionCounts.custom} 条` : ''}</small></div><div class="rule-acquisition-toolbar-actions"><button class="restore-acquisition-recommendations" type="button">已启用规则采用系统建议</button><details class="rule-acquisition-help"><summary>如何选择？</summary><p><strong>智能取证：</strong>适合大多数规则，证据不足时才处理候选图片。<strong>每次执行：</strong>适合签章、勾选、图片材料等不能漏看的事项。<strong>快速／标准／充分：</strong>分别适合单页抽查、常规材料、材料分散或风险较高的情形。</p><p>全局未开启 OCR 或多模态时，能执行的通道仍会继续；不能执行的纯视觉核验会在结果中明确提示。</p></details></div></div>` : '';
+    $('rules').innerHTML = displayRules.length ? `${toolbar}<div class="rule-card-list">${displayRules.map((r) => {
       const checkContent = isDraft ? `<textarea class="rule-check-rule" data-rule="${r.rule_id}" rows="4">${escapeHtml(r.check_rule || r.title)}</textarea>` : `<div class="rule-text">${escapeHtml(r.check_rule || r.title)}</div>`;
-      const visionLabel = {off:'不识图', text_fallback:'文字不足时识图', required:'必须识图'}[r.vision_trigger] || '不识图';
-      const levelLabel = {off:'关闭', low:'快速', standard:'标准', high:'精细'}[r.vision_level] || '关闭';
-      const imageModeLabel = {auto:'自动协同', ocr_only:'仅 OCR', vision_only:'仅多模态', combined:'OCR＋多模态', off:'不使用图片'}[r.image_mode] || '自动协同';
-      const ocrCell = r.vision_trigger !== 'off' ? `<span class="tag">图片取证：${imageModeLabel} · ${visionLabel} · ${levelLabel}</span>` : (r.check_mode === 'ocr' ? '<span class="tag">需要 OCR（暂未选择强度）</span>' : '<span class="muted">无需图片识别</span>');
+      const summary = acquisitionSummary(r);
+      const ocrCell = `<span class="tag acquisition-tag acquisition-${summary.state}" title="${escapeHtml(summary.detail)}">${escapeHtml(summary.label)}</span>`;
       const sourceLabel = r.source_type === 'ai' ? 'AI 提取' : r.source_type === 'global' ? '通用规则库' : ['ai_edited', 'ai_locked'].includes(r.source_type) ? 'AI 提取 · 人工修改' : '人工补充';
       const enabledControl = isDraft ? `<label class="rule-enabled-control" title="仅影响当前规则集；重新提取规则后会重新选择"><input class="rule-enabled" data-rule="${r.rule_id}" type="checkbox" ${r.enabled ? 'checked' : ''}><span>启用</span></label>` : (r.enabled ? '' : '<span class="tag rule-disabled">未启用</span>');
-      const imageActive = r.image_mode !== 'off' && r.vision_trigger !== 'off';
-      const visionControl = isDraft ? `<div class="rule-vision-controls"><div class="rule-vision-heading"><strong>图片取证</strong><small>选定后只影响本条规则；不会改变全局 OCR 或多模态开关。</small></div><label>取证方式<select class="rule-image-mode" data-rule="${r.rule_id}"><option value="auto" ${r.image_mode === 'auto' ? 'selected' : ''}>按规则默认自动判断（推荐）</option><option value="ocr_only" ${r.image_mode === 'ocr_only' ? 'selected' : ''}>只提取图片文字（OCR）</option><option value="vision_only" ${r.image_mode === 'vision_only' ? 'selected' : ''}>只核验图片外观（多模态）</option><option value="combined" ${r.image_mode === 'combined' ? 'selected' : ''}>文字与外观都核验</option><option value="off" ${r.image_mode === 'off' ? 'selected' : ''}>本规则不做图片取证</option></select></label><label>取证强度<select class="rule-vision-level" data-rule="${r.rule_id}" ${imageActive ? '' : 'disabled'}><option value="off" ${r.vision_level === 'off' ? 'selected' : ''}>不执行</option><option value="low" ${r.vision_level === 'low' ? 'selected' : ''}>快速（最多 2 页）</option><option value="standard" ${r.vision_level === 'standard' ? 'selected' : ''}>标准（首批 4 页，可补 4 页）</option><option value="high" ${r.vision_level === 'high' ? 'selected' : ''}>精细（首批 6 页，可补 6 页并找扫描件）</option></select></label><details class="rule-image-advanced"><summary>高级：何时启动图片取证</summary><label>启动条件<select class="rule-vision-trigger" data-rule="${r.rule_id}"><option value="off" ${r.vision_trigger === 'off' ? 'selected' : ''}>不启动</option><option value="text_fallback" ${r.vision_trigger === 'text_fallback' ? 'selected' : ''}>文字证据不足时启动</option><option value="required" ${r.vision_trigger === 'required' ? 'selected' : ''}>本规则必须取证</option></select></label></details><small>OCR：${ocrFeatureConfiguration.enabled ? '已开启（腾讯云优先或本地由全局设置决定）' : '已关闭'}；多模态：${visionConfiguration.enabled ? '已开启，可核验签章、版式等外观事实' : '已关闭'}。选择“只提取图片文字”不会向模型发送图片。</small></div>` : '';
+      const recommendation = r.acquisition_recommendation || {acquisition_preset:'off', vision_level:'off'};
+      const simpleChoice = simpleAcquisitionMode(r);
+      const selectionLevel = ['low', 'standard', 'high'].includes(r.vision_level) ? r.vision_level : 'standard';
+      const strengthControl = simpleChoice === 'off' ? '' : `<label>取证强度<select class="rule-simple-coverage" data-rule="${r.rule_id}"><option value="low" ${selectionLevel === 'low' ? 'selected' : ''}>快速：单页材料或快速抽查</option><option value="standard" ${selectionLevel === 'standard' ? 'selected' : ''}>标准（推荐）：覆盖常见材料与必要补页</option><option value="high" ${selectionLevel === 'high' ? 'selected' : ''}>充分：材料分散、页数较多或风险较高</option></select></label>`;
+      const visionControl = isDraft ? `<div class="rule-vision-controls"><div class="rule-vision-heading"><strong>图片取证</strong><small>日常只需选择“是否启用”和“取证强度”；系统会根据规则内容自动决定 OCR 或图片外观核验。</small></div><div class="rule-acquisition-guide"><strong>怎么选</strong><span>扫描件文字、编号、日期、金额、型号等选“智能取证”；签章、勾选、照片、版式等外观事实也可选“每次执行”；不需要看图片时选“不启用”。</span></div><label>图片取证<select class="rule-simple-acquisition" data-rule="${r.rule_id}"><option value="off" ${simpleChoice === 'off' ? 'selected' : ''}>不启用：仅全文文字审查</option><option value="smart" ${simpleChoice === 'smart' ? 'selected' : ''}>智能取证（推荐）：证据不足时按需启动</option><option value="always" ${simpleChoice === 'always' ? 'selected' : ''}>每次执行：由系统选择必要通道</option><option value="custom" ${simpleChoice === 'custom' ? 'selected' : ''}>专家自定义（查看下方专家模式）</option></select><small>${escapeHtml(acquisitionScene(r))}</small></label>${strengthControl}<div class="rule-acquisition-preview"><strong>执行预览</strong><span>${escapeHtml(acquisitionPreview(r))}</span></div><div class="rule-acquisition-capability">${escapeHtml(acquisitionCapabilityNote())}</div><div class="rule-acquisition-actions"><button class="restore-rule-acquisition" data-rule="${r.rule_id}" type="button">采用系统建议</button><small>系统建议：${escapeHtml(acquisitionPresetLabels[recommendation.acquisition_preset] || '智能取证')} · ${escapeHtml(acquisitionLevelLabels[recommendation.vision_level] || '标准')}强度</small></div><details class="rule-image-advanced"><summary>专家模式：强制通道与启动方式</summary><p class="hint">一般无需修改。仅当需要明确限定“只用 OCR”“只看图片外观”或强制双通道时使用；修改后本规则会显示为“专家自定义”。</p><div class="rule-image-advanced-grid"><label>取证通道<select class="rule-image-mode" data-rule="${r.rule_id}"><option value="auto" ${r.image_mode === 'auto' ? 'selected' : ''}>系统自动选择（推荐）</option><option value="ocr_only" ${r.image_mode === 'ocr_only' ? 'selected' : ''}>仅 OCR：核验扫描文字和字段</option><option value="vision_only" ${r.image_mode === 'vision_only' ? 'selected' : ''}>仅多模态：核验签章、外观等</option><option value="combined" ${r.image_mode === 'combined' ? 'selected' : ''}>OCR＋多模态：双重核验</option><option value="off" ${r.image_mode === 'off' ? 'selected' : ''}>关闭图片取证</option></select></label><label>启动方式<select class="rule-vision-trigger" data-rule="${r.rule_id}"><option value="off" ${r.vision_trigger === 'off' ? 'selected' : ''}>不执行</option><option value="text_fallback" ${r.vision_trigger === 'text_fallback' ? 'selected' : ''}>证据不完整时执行</option><option value="required" ${r.vision_trigger === 'required' ? 'selected' : ''}>每次均执行</option></select></label></div></details></div>` : '';
       return `<details class="rule-card"><summary><span class="rule-card-summary">${enabledControl}<span class="tag">${categoryLabel(r.category)}</span><strong class="rule-card-title">${escapeHtml(r.title)}</strong><span class="tag">${sourceLabel}</span>${ocrCell}</span></summary><div class="rule-card-body"><div class="rule-card-grid"><label>检查规则${checkContent}</label><div class="rule-field"><span class="rule-field-label">招标原文依据</span><div class="rule-text">${escapeHtml(r.source_text || '未提供')}</div></div></div>${visionControl}${isDraft ? `<div class="actions rule-card-actions"><button class="save-check-rule primary" data-rule="${r.rule_id}">保存检查规则</button></div>` : ''}</div></details>`;
     }).join('')}</div>` : '<p class="muted">暂无规则。</p>';
     $('rules').querySelectorAll('details.rule-card').forEach((card, index) => {
@@ -224,14 +283,13 @@
       card.open = Boolean(ruleId && expandedRuleIds.has(ruleId));
     });
     $('rules').querySelectorAll('.rule-enabled').forEach((input) => { input.onclick = (event) => event.stopPropagation(); input.onchange = async () => { try { await request(`/projects/${activeProject}/rules/${input.dataset.rule}`, {method:'PATCH', headers:{'Content-Type':'application/json'}, body:JSON.stringify({enabled:input.checked})}); await refreshRules(); } catch (error) { alert(error.message); await refreshRules(); } }; });
-    const ruleImagePayload = (ruleId, extra = {}) => {
-      // 仅调整交互层级，不改变已存在规则的取证语义：方式、启动条件和强度仍按各自保存值执行。
-      const mode = $('rules').querySelector(`.rule-image-mode[data-rule="${ruleId}"]`)?.value || 'auto';
-      const trigger = $('rules').querySelector(`.rule-vision-trigger[data-rule="${ruleId}"]`)?.value || 'off';
-      const level = $('rules').querySelector(`.rule-vision-level[data-rule="${ruleId}"]`)?.value || 'off';
-      return {...extra, image_mode:mode, vision_trigger:mode === 'off' ? 'off' : trigger, vision_level:(mode === 'off' || trigger === 'off') ? 'off' : level};
-    };
-    $('rules').querySelectorAll('.rule-image-mode, .rule-vision-trigger, .rule-vision-level').forEach((input) => { input.onchange = async () => { try { await request(`/projects/${activeProject}/rules/${input.dataset.rule}`, {method:'PATCH', headers:{'Content-Type':'application/json'}, body:JSON.stringify(ruleImagePayload(input.dataset.rule))}); await refreshRules(); } catch (error) { alert(error.message); await refreshRules(); } }; });
+    const ruleById = new Map(displayRules.map((rule) => [rule.rule_id, rule]));
+    const saveAcquisition = async (ruleId, payload) => request(`/projects/${activeProject}/rules/${ruleId}`, {method:'PATCH', headers:{'Content-Type':'application/json'}, body:JSON.stringify(payload)});
+    $('rules').querySelectorAll('.rule-simple-acquisition').forEach((input) => { input.onchange = async () => { const rule = ruleById.get(input.dataset.rule); if (input.value === 'custom') { const advanced = input.closest('.rule-vision-controls')?.querySelector('.rule-image-advanced'); if (advanced) advanced.open = true; input.value = simpleAcquisitionMode(rule); return; } try { const level = input.closest('.rule-vision-controls')?.querySelector('.rule-simple-coverage')?.value || 'standard'; await saveAcquisition(input.dataset.rule, simpleAcquisitionPayload(input.value, level, rule)); await refreshRules(); } catch (error) { alert(error.message); await refreshRules(); } }; });
+    $('rules').querySelectorAll('.rule-simple-coverage').forEach((input) => { input.onchange = async () => { try { const control = input.closest('.rule-vision-controls'); const choice = control?.querySelector(`.rule-simple-acquisition[data-rule="${input.dataset.rule}"]`)?.value || 'smart'; const rule = ruleById.get(input.dataset.rule); if (choice === 'custom') { const mode = control?.querySelector(`.rule-image-mode[data-rule="${input.dataset.rule}"]`)?.value || 'auto'; const trigger = control?.querySelector(`.rule-vision-trigger[data-rule="${input.dataset.rule}"]`)?.value || 'off'; await saveAcquisition(input.dataset.rule, {acquisition_preset:'custom', image_mode:mode, vision_trigger:trigger, vision_level:(mode === 'off' || trigger === 'off') ? 'off' : input.value}); } else { await saveAcquisition(input.dataset.rule, simpleAcquisitionPayload(choice, input.value, rule)); } await refreshRules(); } catch (error) { alert(error.message); await refreshRules(); } }; });
+    $('rules').querySelectorAll('.rule-image-mode, .rule-vision-trigger').forEach((input) => { input.onchange = async () => { try { const control = input.closest('.rule-vision-controls'); const level = control?.querySelector(`.rule-simple-coverage[data-rule="${input.dataset.rule}"]`)?.value || 'standard'; const mode = control?.querySelector(`.rule-image-mode[data-rule="${input.dataset.rule}"]`)?.value || 'auto'; const trigger = control?.querySelector(`.rule-vision-trigger[data-rule="${input.dataset.rule}"]`)?.value || 'off'; await saveAcquisition(input.dataset.rule, {acquisition_preset:'custom', image_mode:mode, vision_trigger:trigger, vision_level:(mode === 'off' || trigger === 'off') ? 'off' : level}); await refreshRules(); } catch (error) { alert(error.message); await refreshRules(); } }; });
+    $('rules').querySelectorAll('.restore-rule-acquisition').forEach((button) => button.onclick = async () => { try { const rule = ruleById.get(button.dataset.rule); const recommendation = rule?.acquisition_recommendation || {acquisition_preset:'off', vision_level:'off'}; await saveAcquisition(button.dataset.rule, presetPayload(recommendation.acquisition_preset, recommendation.vision_level, rule)); await refreshRules(); } catch (error) { alert(error.message); await refreshRules(); } });
+    $('rules').querySelectorAll('.restore-acquisition-recommendations').forEach((button) => button.onclick = async () => { const candidates = displayRules.filter((rule) => rule.enabled && rule.acquisition_recommendation); if (!candidates.length || !confirm(`将为 ${candidates.length} 条已启用规则恢复系统建议；已有自定义取证设置会被替换。是否继续？`)) return; button.disabled = true; try { for (const rule of candidates) { const recommendation = rule.acquisition_recommendation; await saveAcquisition(rule.rule_id, presetPayload(recommendation.acquisition_preset, recommendation.vision_level, rule)); } await refreshRules(); } catch (error) { alert(error.message); await refreshRules(); } finally { button.disabled = false; } });
     $('rules').querySelectorAll('.save-check-rule').forEach((button) => button.onclick = async () => { try { const input = $('rules').querySelector(`.rule-check-rule[data-rule="${button.dataset.rule}"]`); await request(`/projects/${activeProject}/rules/${button.dataset.rule}`, {method:'PATCH', headers:{'Content-Type':'application/json'}, body:JSON.stringify({check_rule:input.value})}); await refreshRules(); } catch (error) { alert(error.message); } });
   }
   function groupByBidder(results) { const groups = new Map(); results.forEach((item) => { const bidder = item.bidder_name || item.original_name || '未填写投标人'; if (!groups.has(bidder)) groups.set(bidder, []); groups.get(bidder).push(item); }); return [...groups.entries()]; }
@@ -363,16 +421,17 @@
   $('manual-rule-category').onchange = updateManualRuleScoringFields;
   updateManualRuleScoringFields();
   $('add-manual-rule').onclick = async () => { try { const category = $('manual-rule-category').value; const isScoring = ['objective', 'subjective'].includes(category); const rawMaxScore = $('manual-rule-max-score').value; const maxScore = Number(rawMaxScore); if (isScoring && (!Number.isFinite(maxScore) || maxScore <= 0)) { alert('客观分和主观分规则必须填写大于 0 的满分。'); return; } const payload = {category, title:$('manual-rule-title').value, check_rule:$('manual-rule-check').value, source_text:$('manual-rule-source').value, ocr_required:$('manual-rule-ocr').checked}; if (isScoring) payload.scoring = {max_score:maxScore, kind:category === 'objective' ? $('manual-rule-score-kind').value : 'manual'}; await request(`/projects/${activeProject}/rules`, {method:'POST', headers:{'Content-Type':'application/json'}, body:JSON.stringify(payload)}); $('manual-rule-title').value = ''; $('manual-rule-check').value = ''; $('manual-rule-source').value = ''; $('manual-rule-max-score').value = ''; $('manual-rule-ocr').checked = false; updateManualRuleScoringFields(); await refreshRules(); } catch (error) { alert(error.message); } };
-  $('confirm-rules').onclick = async () => { try { await request(`/projects/${activeProject}/rules/confirm`, {method:'POST'}); await refreshRules(); } catch (error) { alert(error.message); } };
+  $('confirm-rules').onclick = async () => { try { const validation = await request(`/projects/${activeProject}/rules/acquisition-validation`); const issues = Array.isArray(validation.issues) ? validation.issues : []; if (issues.length) { const brief = issues.slice(0, 6).map((item) => `• ${item.title}：${item.message}`).join('\n'); const extra = issues.length > 6 ? `\n另有 ${issues.length - 6} 条提示。` : ''; if (!confirm(`图片取证预检发现 ${issues.length} 条提示：\n${brief}${extra}\n\n仍要确认当前规则集吗？`)) return; } await request(`/projects/${activeProject}/rules/confirm`, {method:'POST'}); await refreshRules(); } catch (error) { alert(error.message); } };
   $('start-evaluate-all').onclick = async () => { try { const profile_id = $('all-profile').value; const rulesData = await request(`/projects/${activeProject}/rules`); const visionRules = rulesData.rules.filter((rule) => rule.enabled && rule.vision_trigger !== 'off' && rule.vision_level !== 'off' && !['ocr_only', 'off'].includes(rule.image_mode || 'auto')); if (visionConfiguration.enabled && visionRules.length) { const selected = modelProfiles.find((item) => item.profile_id === profile_id); const fallback = modelProfiles.find((item) => item.profile_id === visionConfiguration.default_profile_id && item.enabled && item.capabilities?.vision); if (!selected?.capabilities?.vision && fallback && !confirm(`当前评审模型“${selected?.display_name || '所选模型'}”不是多模态模型；仅需要图片外观核验的规则将改用“${fallback.display_name}”，文字评审与 OCR 仍使用当前模型。是否继续？`)) return; } await request(`/projects/${activeProject}/tasks`, {method:'POST', headers:{'Content-Type':'application/json'}, body:JSON.stringify({task_type:'evaluate_all', profile_id})}); await refreshProject(); await Promise.all([refreshReview(), refreshScores(), refreshUsage()]); } catch (error) { alert(error.message); } };
   function closeModels() { $('models-panel').classList.add('hidden'); document.body.classList.remove('modal-open'); resetModelForm(); }
   function closePrompts() { $('prompts-panel').classList.add('hidden'); document.body.classList.remove('modal-open'); }
-  function resetGlobalRuleForm() { $('global-rule-id').value = ''; $('global-rule-form-title').textContent = '新增通用规则'; $('global-rule-category').value = 'substantive'; $('global-rule-title').value = ''; $('global-rule-check').value = ''; $('global-rule-source').value = ''; $('global-rule-ocr').checked = false; $('global-rule-enabled').checked = true; }
+  function syncGlobalRuleAcquisitionControls() { const off = $('global-rule-acquisition').value === 'off'; $('global-rule-coverage-field').classList.toggle('is-hidden', off); }
+  function resetGlobalRuleForm() { $('global-rule-id').value = ''; $('global-rule-form-title').textContent = '新增通用规则'; $('global-rule-category').value = 'substantive'; $('global-rule-title').value = ''; $('global-rule-check').value = ''; $('global-rule-source').value = ''; $('global-rule-acquisition').value = 'off'; $('global-rule-coverage').value = 'standard'; $('global-rule-ocr').checked = false; $('global-rule-enabled').checked = true; syncGlobalRuleAcquisitionControls(); }
   function closeGlobalRules() { $('global-rules-panel').classList.add('hidden'); document.body.classList.remove('modal-open'); resetGlobalRuleForm(); }
   async function loadGlobalRules() {
     const data = await request('/global-rules'); globalRules = data.rules;
-    $('global-rules').innerHTML = globalRules.length ? `<div class="model-profile-cards">${globalRules.map((rule) => `<article class="global-rule-card"><div><h4>${escapeHtml(rule.title)} ${rule.enabled ? '<span class="tag">默认选择</span>' : '<span class="muted">默认不选</span>'}</h4><p><span class="tag">${categoryLabel(rule.category)}</span>${rule.check_mode === 'ocr' ? ' <span class="tag">需 OCR</span>' : ''}</p><p><strong>检查规则：</strong>${escapeHtml(rule.check_rule)}</p>${rule.source_text ? `<p class="muted">招标原文依据：${escapeHtml(rule.source_text)}</p>` : ''}</div><div class="model-profile-actions"><button class="edit-global-rule" data-rule="${rule.global_rule_id}">编辑</button><button class="delete-global-rule danger" data-rule="${rule.global_rule_id}">删除</button></div></article>`).join('')}</div>` : '<p class="muted">暂无通用规则。保存后会自动导入今后新建的项目。</p>';
-    $('global-rules').querySelectorAll('.edit-global-rule').forEach((button) => button.onclick = () => { const rule = globalRules.find((item) => item.global_rule_id === button.dataset.rule); if (!rule) return; $('global-rule-id').value = rule.global_rule_id; $('global-rule-form-title').textContent = `编辑通用规则：${rule.title}`; $('global-rule-category').value = rule.category; $('global-rule-title').value = rule.title; $('global-rule-check').value = rule.check_rule; $('global-rule-source').value = rule.source_text || ''; $('global-rule-ocr').checked = rule.check_mode === 'ocr'; $('global-rule-enabled').checked = Boolean(rule.enabled); });
+    $('global-rules').innerHTML = globalRules.length ? `<div class="model-profile-cards">${globalRules.map((rule) => { const summary = acquisitionSummary(rule); return `<article class="global-rule-card"><div><h4>${escapeHtml(rule.title)} ${rule.enabled ? '<span class="tag">默认选择</span>' : '<span class="muted">默认不选</span>'}</h4><p><span class="tag">${categoryLabel(rule.category)}</span> <span class="tag acquisition-${summary.state}">${escapeHtml(summary.label)}</span></p><p><strong>检查规则：</strong>${escapeHtml(rule.check_rule)}</p>${rule.source_text ? `<p class="muted">招标原文依据：${escapeHtml(rule.source_text)}</p>` : ''}</div><div class="model-profile-actions"><button class="edit-global-rule" data-rule="${rule.global_rule_id}">编辑</button><button class="delete-global-rule danger" data-rule="${rule.global_rule_id}">删除</button></div></article>`; }).join('')}</div>` : '<p class="muted">暂无通用规则。保存后会自动导入今后新建的项目。</p>';
+    $('global-rules').querySelectorAll('.edit-global-rule').forEach((button) => button.onclick = () => { const rule = globalRules.find((item) => item.global_rule_id === button.dataset.rule); if (!rule) return; $('global-rule-id').value = rule.global_rule_id; $('global-rule-form-title').textContent = `编辑通用规则：${rule.title}`; $('global-rule-category').value = rule.category; $('global-rule-title').value = rule.title; $('global-rule-check').value = rule.check_rule; $('global-rule-source').value = rule.source_text || ''; $('global-rule-acquisition').value = simpleAcquisitionMode(rule); $('global-rule-coverage').value = ['low', 'standard', 'high'].includes(rule.vision_level) ? rule.vision_level : 'standard'; $('global-rule-ocr').checked = rule.check_mode === 'ocr'; $('global-rule-enabled').checked = Boolean(rule.enabled); syncGlobalRuleAcquisitionControls(); });
     $('global-rules').querySelectorAll('.delete-global-rule').forEach((button) => button.onclick = async () => { if (!confirm('删除此通用规则不会影响已有项目；是否继续？')) return; const password = window.prompt('请输入通用规则库操作口令'); if (password === null) return; try { await request(`/global-rules/${button.dataset.rule}`, {method:'DELETE', headers:{'Content-Type':'application/json'}, body:JSON.stringify({password})}); if ($('global-rule-id').value === button.dataset.rule) resetGlobalRuleForm(); await loadGlobalRules(); } catch (error) { alert(error.message); } });
   }
   function promptTemplateCard(item, open = false) {
@@ -408,7 +467,9 @@
   $('close-global-rules').onclick = closeGlobalRules;
   // 配置弹窗仅允许通过各自的“关闭 ×”按钮退出，避免误点遮罩或 Esc 丢失正在编辑的内容。
   $('reset-global-rule-form').onclick = resetGlobalRuleForm;
-  $('save-global-rule').onclick = async () => { const password = window.prompt('请输入通用规则库操作口令'); if (password === null) return; try { const ruleId = $('global-rule-id').value; const payload = {category:$('global-rule-category').value, title:$('global-rule-title').value, check_rule:$('global-rule-check').value, source_text:$('global-rule-source').value, ocr_required:$('global-rule-ocr').checked, enabled:$('global-rule-enabled').checked, password}; await request(ruleId ? `/global-rules/${ruleId}` : '/global-rules', {method:ruleId ? 'PATCH' : 'POST', headers:{'Content-Type':'application/json'}, body:JSON.stringify(payload)}); resetGlobalRuleForm(); await loadGlobalRules(); } catch (error) { alert(error.message); } };
+  $('global-rule-acquisition').onchange = syncGlobalRuleAcquisitionControls;
+  syncGlobalRuleAcquisitionControls();
+  $('save-global-rule').onclick = async () => { const password = window.prompt('请输入通用规则库操作口令'); if (password === null) return; try { const ruleId = $('global-rule-id').value; const preset = $('global-rule-acquisition').value; const level = $('global-rule-coverage').value; const existing = globalRules.find((rule) => rule.global_rule_id === ruleId); const payload = {category:$('global-rule-category').value, title:$('global-rule-title').value, check_rule:$('global-rule-check').value, source_text:$('global-rule-source').value, ocr_required:$('global-rule-ocr').checked, enabled:$('global-rule-enabled').checked, password, ...presetPayload(preset, level, existing)}; const data = await request(ruleId ? `/global-rules/${ruleId}` : '/global-rules', {method:ruleId ? 'PATCH' : 'POST', headers:{'Content-Type':'application/json'}, body:JSON.stringify(payload)}); resetGlobalRuleForm(); await loadGlobalRules(); if (!ruleId && activeProject) await refreshRules(); if (!ruleId && data.rule?.synced_draft_rule_sets) $('global-rule-form-title').textContent = `已同步至 ${data.rule.synced_draft_rule_sets} 个待确认项目`; } catch (error) { alert(error.message); } };
   $('reset-model-form').onclick = resetModelForm;
   $('model-preset').onchange = applyModelPreset;
   $('save-model-profile').onclick = async () => { try { const profileId = $('model-profile-id').value; const modelName = $('model-name').value; const payload = {display_name:$('model-display-name').value, base_url:$('model-base-url').value, model_name:modelName, api_key:$('model-api-key').value, json_mode:$('model-json-mode').value === 'true', thinking_mode:normalizedThinkingMode(modelName, $('model-thinking').value), supports_vision:$('model-supports-vision').checked}; await request(profileId ? `/model-profiles/${profileId}` : '/model-profiles', {method:profileId ? 'PATCH' : 'POST', headers:{'Content-Type':'application/json'}, body:JSON.stringify(payload)}); resetModelForm(); await loadProfiles(); } catch (error) { alert(error.message); } };
diff --git a/dashboard/static/evaluation_workbench/style.css b/dashboard/static/evaluation_workbench/style.css
index 0cd6855..d1e5c2b 100644
--- a/dashboard/static/evaluation_workbench/style.css
+++ b/dashboard/static/evaluation_workbench/style.css
@@ -9,7 +9,7 @@ button { border:1px solid #cbd5e1; border-radius:8px; padding:9px 13px; cursor:p
 .result-evidence { white-space:pre-wrap; overflow-wrap:anywhere; }.result-evidence + .result-evidence { display:block; margin-top:5px; }
 .vision-result { margin-top:8px; padding:7px 9px; border:1px solid #dbe5f1; border-left-width:4px; border-radius:7px; background:#f8fafc; color:#475569; font-size:12px; line-height:1.45; }.vision-result strong,.vision-result span,.vision-result small { display:block; }.vision-result strong { color:#334155; font-size:13px; font-weight:800; }.vision-result-applied,.vision-result-applied_partial { border-color:#86efac; background:#f0fdf4; }.vision-result-applied strong,.vision-result-applied_partial strong { color:#15803d; }.vision-result-conflict { border-color:#fca5a5; background:#fff1f2; }.vision-result-conflict strong { color:#b91c1c; }.vision-result-uncovered,.vision-result-not_located,.vision-result-unavailable { border-color:#fcd34d; background:#fffbeb; }.vision-result-uncovered strong,.vision-result-not_located strong,.vision-result-unavailable strong { color:#a16207; }.vision-result-failed { border-color:#fecaca; background:#fef2f2; }.vision-result-failed strong { color:#b91c1c; }.vision-badge { display:inline-block; margin-top:6px; padding:3px 7px; border-radius:999px; font-size:12px; font-weight:800; line-height:1.35; background:#e2e8f0; color:#475569; }.vision-badge-applied,.vision-badge-applied_partial { background:#dcfce7; color:#15803d; }.vision-badge-conflict,.vision-badge-failed { background:#fee2e2; color:#b91c1c; }.vision-badge-uncovered { background:#fef3c7; color:#a16207; }
 .review-result-table-wrap { overflow-x:auto; }.review-result-table { min-width:920px; table-layout:fixed; }.review-result-table .review-col-category { width:10%; }.review-result-table .review-col-rule { width:23%; }.review-result-table .review-col-advice { width:17%; }.review-result-table .review-col-risk { width:9%; }.review-result-table .review-col-evidence { width:41%; }.review-result-table td:nth-child(2) { color:#475569; font-size:14px; }.review-result-table td:nth-child(3),.review-result-table td:nth-child(5) { overflow-wrap:anywhere; }.review-result-table td:nth-child(5) { background:#fbfdff; }.review-result-table td:nth-child(5) .result-evidence { max-width:none; }
-.rule-card-list { display:grid; gap:10px; }.rule-card { border:1px solid var(--line); border-radius:10px; background:#fff; overflow:hidden; }.rule-card summary { padding:12px 14px; cursor:pointer; list-style:none; }.rule-card summary::-webkit-details-marker { display:none; }.rule-card summary::before { content:'›'; display:inline-block; margin-right:8px; color:var(--muted); font-size:20px; line-height:1; vertical-align:middle; transition:transform .15s ease; }.rule-card[open] summary::before { transform:rotate(90deg); }.rule-card-summary { display:inline-flex; width:calc(100% - 28px); align-items:center; gap:8px; vertical-align:middle; }.rule-enabled-control { display:inline-flex; grid-template-columns:none; align-items:center; gap:4px; color:#334155; font-size:13px; font-weight:600; white-space:nowrap; cursor:pointer; }.rule-enabled-control input { width:auto; margin:0; }.rule-disabled { background:#f1f5f9; color:#64748b; }.rule-card-title { flex:1; min-width:0; overflow-wrap:anywhere; }.rule-card-body { padding:14px; border-top:1px solid var(--line); background:#fbfdff; }.rule-card-grid { display:grid; grid-template-columns:repeat(2,minmax(0,1fr)); gap:14px; }.rule-field { display:grid; gap:6px; color:#334155; font-weight:600; }.rule-field-label { display:block; }.rule-text { min-height:42px; padding:9px 10px; border:1px solid #dbe4ee; border-radius:7px; background:#fff; color:#334155; font-weight:400; white-space:pre-wrap; overflow-wrap:anywhere; }.rule-card-actions { margin:14px 0 0; }.rule-vision-controls { display:grid; grid-template-columns:repeat(2,minmax(200px,290px)); gap:10px 14px; align-items:end; margin-top:14px; padding:14px; border:1px solid #bfdbfe; border-radius:10px; background:#f8fbff; }.rule-vision-heading { grid-column:1/-1; display:grid; gap:2px; }.rule-vision-heading strong { color:#1e3a5f; }.rule-vision-heading small,.rule-vision-controls>small { color:var(--muted); }.rule-vision-controls label { display:grid; gap:5px; color:#334155; font-size:13px; font-weight:600; }.rule-vision-controls>small { grid-column:1/-1; }.rule-image-advanced { grid-column:1/-1; padding-top:2px; }.rule-image-advanced summary { cursor:pointer; color:#475569; font-size:13px; font-weight:700; }.rule-image-advanced label { max-width:290px; margin-top:9px; }
+.rule-card-list { display:grid; gap:10px; }.rule-card { border:1px solid var(--line); border-radius:10px; background:#fff; overflow:hidden; }.rule-card summary { padding:12px 14px; cursor:pointer; list-style:none; }.rule-card summary::-webkit-details-marker { display:none; }.rule-card summary::before { content:'›'; display:inline-block; margin-right:8px; color:var(--muted); font-size:20px; line-height:1; vertical-align:middle; transition:transform .15s ease; }.rule-card[open] summary::before { transform:rotate(90deg); }.rule-card-summary { display:inline-flex; width:calc(100% - 28px); align-items:center; gap:8px; vertical-align:middle; }.rule-enabled-control { display:inline-flex; grid-template-columns:none; align-items:center; gap:4px; color:#334155; font-size:13px; font-weight:600; white-space:nowrap; cursor:pointer; }.rule-enabled-control input { width:auto; margin:0; }.rule-disabled { background:#f1f5f9; color:#64748b; }.rule-card-title { flex:1; min-width:0; overflow-wrap:anywhere; }.rule-card-body { padding:14px; border-top:1px solid var(--line); background:#fbfdff; }.rule-card-grid { display:grid; grid-template-columns:repeat(2,minmax(0,1fr)); gap:14px; }.rule-field { display:grid; gap:6px; color:#334155; font-weight:600; }.rule-field-label { display:block; }.rule-text { min-height:42px; padding:9px 10px; border:1px solid #dbe4ee; border-radius:7px; background:#fff; color:#334155; font-weight:400; white-space:pre-wrap; overflow-wrap:anywhere; }.rule-card-actions { margin:14px 0 0; }.rule-acquisition-toolbar { display:flex; align-items:center; justify-content:space-between; flex-wrap:wrap; gap:10px; margin:0 0 12px; padding:10px 12px; border:1px solid #dbeafe; border-radius:9px; background:#f8fbff; color:#475569; font-size:13px; }.acquisition-tag { max-width:260px; overflow:hidden; text-overflow:ellipsis; white-space:nowrap; }.acquisition-warning { background:#fff7ed; color:#9a3412; }.acquisition-active { background:#e0f2fe; color:#075985; }.acquisition-off { background:#f1f5f9; color:#64748b; }.rule-vision-controls { display:grid; grid-template-columns:repeat(2,minmax(200px,290px)); gap:10px 14px; align-items:end; margin-top:14px; padding:14px; border:1px solid #bfdbfe; border-radius:10px; background:#f8fbff; }.rule-vision-heading { grid-column:1/-1; display:grid; gap:2px; }.rule-vision-heading strong { color:#1e3a5f; }.rule-vision-heading small,.rule-vision-controls>small { color:var(--muted); }.rule-vision-controls label { display:grid; gap:5px; color:#334155; font-size:13px; font-weight:600; }.rule-vision-controls>small { grid-column:1/-1; }.rule-acquisition-preview { grid-column:1/-1; display:grid; gap:3px; padding:9px 10px; border-radius:7px; background:#eef6ff; color:#334155; font-size:13px; }.rule-acquisition-preview strong { color:#1e3a5f; }.rule-acquisition-actions { grid-column:1/-1; display:flex; align-items:center; flex-wrap:wrap; gap:8px; }.rule-acquisition-actions small { color:var(--muted); }.rule-image-advanced { grid-column:1/-1; padding-top:2px; }.rule-image-advanced summary { cursor:pointer; color:#475569; font-size:13px; font-weight:700; }.rule-image-advanced-grid { display:grid; grid-template-columns:repeat(2,minmax(200px,290px)); gap:10px 14px; margin-top:9px; }.rule-image-advanced label { margin:0; }
 table { width:100%; border-collapse:collapse; } th,td { text-align:left; padding:10px; border-bottom:1px solid var(--line); vertical-align:top; } th { color:var(--muted); font-size:13px; }.tag { display:inline-block; padding:2px 7px; border-radius:99px; background:#e0ecff; color:#1d4ed8; font-size:12px; }.status-success { color:#15803d; }.status-error { color:#dc2626; }.status-running,.status-queued { color:#b45309; }
 .queue-detail { display:inline-block; max-width:760px; margin-top:5px; color:#7c5a16; line-height:1.5; }
 .decision-boundary { margin:0 0 12px; padding:11px 13px; border-left:4px solid #f97316; background:#fff7ed; color:#9a3412; }.priority-high { color:#b91c1c; font-weight:700; }.priority-medium { color:#b45309; font-weight:700; }.priority-normal { color:#1d4ed8; }.priority-none { color:var(--muted); }.evidence { margin:7px 0 0; padding:8px; max-width:520px; white-space:pre-wrap; overflow-wrap:anywhere; background:#f8fafc; border-radius:6px; font:12px/1.45 ui-monospace,SFMono-Regular,Consolas,monospace; }.signal-note { min-width:180px; margin:6px 0; resize:vertical; }
@@ -22,4 +22,8 @@ table { width:100%; border-collapse:collapse; } th,td { text-align:left; padding
 .vision-result-applied_partial,.vision-result-ocr_applied_partial,.vision-result-ocr_vision_applied_partial { border-color:#fcd34d; background:#fffbeb; }.vision-result-applied_partial strong,.vision-result-ocr_applied_partial strong,.vision-result-ocr_vision_applied_partial strong { color:#a16207; }.vision-badge-applied_partial,.vision-badge-ocr_applied_partial,.vision-badge-ocr_vision_applied_partial { background:#fef3c7; color:#a16207; }.evidence-chain { margin-top:8px; border-top:1px dashed #cbd5e1; padding-top:6px; color:#475569; font-size:12px; }.evidence-chain summary { cursor:pointer; font-weight:700; color:#475569; }.evidence-layer { margin-top:7px; padding:6px 8px; border-radius:6px; background:#f8fafc; }.evidence-layer strong,.evidence-layer small,.evidence-layer span { display:block; }.evidence-layer small { color:#64748b; margin:2px 0; }.evidence-layer span { white-space:pre-wrap; }
 .global-rule-card { display:grid; grid-template-columns:minmax(0,1fr) auto; gap:16px; padding:15px; border:1px solid var(--line); border-radius:10px; background:#fff; }.global-rule-card h4,.global-rule-card p { margin:0 0 8px; }.global-rule-card p:last-child { margin-bottom:0; overflow-wrap:anywhere; }
 .prompt-modal { width:min(1040px,100%); max-height:calc(100vh - 48px); margin:0; padding:0; overflow:hidden; display:flex; flex-direction:column; }.prompt-template-list { flex:1 1 auto; min-height:0; overflow-y:auto; overflow-x:hidden; padding:20px 22px; display:flex; flex-direction:column; gap:16px; background:#f8fafc; }.prompt-config-group { flex:0 0 auto; border:1px solid var(--line); border-radius:12px; background:#fff; overflow:hidden; }.prompt-config-group>summary,.prompt-group-heading { display:flex; justify-content:space-between; align-items:center; gap:18px; padding:16px 18px; cursor:pointer; list-style:none; }.prompt-config-group>summary::-webkit-details-marker,.prompt-template-card>summary::-webkit-details-marker { display:none; }.prompt-config-group>summary h3,.prompt-group-heading h3 { margin:0; }.prompt-config-group>summary p,.prompt-group-heading p { margin:3px 0 0; color:var(--muted); }.prompt-config-group>summary::before { content:'›'; margin-right:-8px; color:var(--muted); font-size:22px; transition:transform .15s ease; }.prompt-config-group[open]>summary::before { transform:rotate(90deg); }.prompt-config-business { border-color:#bfdbfe; }.prompt-config-business .prompt-group-heading { background:#eff6ff; }.prompt-config-workflow>summary { background:#fffaf0; }.prompt-config-system>summary { background:#f1f5f9; }.prompt-group-count { flex:0 0 auto; padding:3px 9px; border-radius:99px; background:#e2e8f0; color:#475569; font-size:12px; }.prompt-group-body { padding:0 16px 16px; }.prompt-warning { margin:0 0 14px; padding:10px 12px; border-left:4px solid #f59e0b; background:#fffbeb; color:#92400e; }.prompt-warning-system { border-left-color:#64748b; background:#f8fafc; color:#475569; }.prompt-workflow-section { padding:14px 16px 16px; border-top:1px solid var(--line); }.prompt-group-body .prompt-workflow-section { padding-right:0; padding-left:0; }.prompt-workflow-section h4 { margin:0 0 9px; color:#475569; font-size:13px; }.prompt-section-cards { display:grid; gap:9px; }.prompt-template-card { border:1px solid var(--line); border-radius:9px; background:#fff; overflow:hidden; }.prompt-template-card>summary { display:flex; align-items:center; gap:8px; padding:11px 13px; cursor:pointer; list-style:none; }.prompt-template-card>summary::before { content:'›'; color:var(--muted); font-size:20px; line-height:1; transition:transform .15s ease; }.prompt-template-card[open]>summary::before { transform:rotate(90deg); }.prompt-template-title { flex:1; min-width:0; font-weight:700; overflow-wrap:anywhere; }.prompt-level-badge { flex:0 0 auto; padding:2px 7px; border-radius:99px; font-size:12px; }.prompt-level-recommended { border-color:#bfdbfe; }.prompt-level-recommended .prompt-level-badge { background:#dcfce7; color:#166534; }.prompt-level-careful .prompt-level-badge { background:#fef3c7; color:#92400e; }.prompt-level-advanced .prompt-level-badge { background:#e2e8f0; color:#475569; }.prompt-template-body { padding:0 13px 14px; border-top:1px solid var(--line); background:#fbfdff; }.prompt-template-card .hint { margin:10px 0; }.prompt-placeholders { margin:0 0 10px; padding:8px 10px; border-radius:7px; color:#7c2d12; background:#fff7ed; font:12px/1.5 ui-monospace,SFMono-Regular,Consolas,monospace; overflow-wrap:anywhere; }.prompt-template-content { min-height:150px; resize:vertical; font:13px/1.5 ui-monospace,SFMono-Regular,Consolas,monospace; }
-@media(max-width:760px){.topbar,.workspace-title{flex-direction:column;}.shell{padding:18px 12px;}.modal-backdrop{padding:12px;}.model-modal{max-height:calc(100vh - 24px);}.model-config-layout{grid-template-columns:1fr; overflow:auto;}.model-editor{overflow:visible; border-right:0; border-bottom:1px solid var(--line);}.model-list{overflow:visible;}.model-profiles-scroll{overflow:visible;}.model-profile-card,.global-rule-card,.rule-card-grid,.rule-vision-controls{grid-template-columns:1fr;}.model-profile-details{grid-template-columns:1fr;}.model-profile-actions{flex-direction:row; flex-wrap:wrap; align-items:center;}.model-profile-actions .muted{text-align:left;}.rule-card-summary{align-items:flex-start; flex-wrap:wrap;}.rule-card-title{flex-basis:calc(100% - 92px);}}
+@media(max-width:760px){.topbar,.workspace-title{flex-direction:column;}.shell{padding:18px 12px;}.modal-backdrop{padding:12px;}.model-modal{max-height:calc(100vh - 24px);}.model-config-layout{grid-template-columns:1fr; overflow:auto;}.model-editor{overflow:visible; border-right:0; border-bottom:1px solid var(--line);}.model-list{overflow:visible;}.model-profiles-scroll{overflow:visible;}.model-profile-card,.global-rule-card,.rule-card-grid,.rule-vision-controls,.rule-image-advanced-grid{grid-template-columns:1fr;}.model-profile-details{grid-template-columns:1fr;}.model-profile-actions{flex-direction:row; flex-wrap:wrap; align-items:center;}.model-profile-actions .muted{text-align:left;}.rule-card-summary{align-items:flex-start; flex-wrap:wrap;}.rule-card-title{flex-basis:calc(100% - 92px);}.rule-acquisition-toolbar{align-items:flex-start;}}
+
+.is-hidden { display:none !important; }
+.rule-acquisition-toolbar > div:first-child { display:grid; gap:4px; min-width:0; }.rule-acquisition-toolbar > div:first-child small { color:var(--muted); }.rule-acquisition-toolbar-actions { display:flex; align-items:flex-start; justify-content:flex-end; flex-wrap:wrap; gap:8px; }.rule-acquisition-help { position:relative; }.rule-acquisition-help summary { cursor:pointer; color:#475569; font-size:13px; font-weight:700; }.rule-acquisition-help p { position:absolute; z-index:4; right:0; width:min(520px,calc(100vw - 64px)); margin:7px 0 0; padding:10px 12px; border:1px solid #bfdbfe; border-radius:8px; background:#fff; color:#334155; box-shadow:0 8px 20px rgba(15,23,42,.12); font-size:12px; line-height:1.55; }.rule-acquisition-help p + p { margin-top:106px; }.rule-acquisition-guide { grid-column:1/-1; display:grid; grid-template-columns:auto 1fr; gap:6px 10px; padding:9px 10px; border-left:4px solid #60a5fa; border-radius:7px; background:#eff6ff; color:#334155; font-size:13px; line-height:1.5; }.rule-acquisition-guide strong { color:#1e3a5f; }.rule-vision-controls label small { color:var(--muted); font-weight:400; line-height:1.45; }.rule-acquisition-capability { grid-column:1/-1; padding:8px 10px; border:1px dashed #cbd5e1; border-radius:7px; color:#475569; background:#fff; font-size:12px; line-height:1.45; }.rule-image-advanced .hint { margin:8px 0 0; color:#64748b; font-size:12px; line-height:1.5; }
+@media(max-width:760px){.rule-acquisition-toolbar-actions{justify-content:flex-start;}.rule-acquisition-help p{left:0; right:auto; width:min(520px,calc(100vw - 42px));}.rule-acquisition-help p + p{margin-top:124px;}.rule-acquisition-guide{grid-template-columns:1fr;}}
diff --git a/dashboard/templates/evaluation_workbench/index.html b/dashboard/templates/evaluation_workbench/index.html
index 90c3f78..f0a4455 100644
--- a/dashboard/templates/evaluation_workbench/index.html
+++ b/dashboard/templates/evaluation_workbench/index.html
@@ -35,7 +35,7 @@
             <section class="panel model-modal">
                 <div class="workspace-title model-modal-header"><div><h2 id="global-rules-title">全局通用规则库</h2><p class="muted">全部规则都会在新建项目或重新提取规则时复制到待确认规则集；“默认选择”只决定进入项目后的初始勾选状态。新增、修改或删除时需要输入操作口令；后续修改不会影响已创建项目。</p></div><button id="close-global-rules" class="link">关闭 ×</button></div>
                 <div class="model-config-layout">
-                    <article class="model-editor"><h3 id="global-rule-form-title">新增通用规则</h3><input id="global-rule-id" type="hidden"><label>分类<select id="global-rule-category"><option value="qualification">资格性</option><option value="compliance">符合性</option><option value="substantive" selected>实质性/废标项</option><option value="other">其他规则</option></select></label><label>规则名称<input id="global-rule-title" placeholder="例如：营业执照有效性"></label><label>检查规则<textarea id="global-rule-check" rows="4" placeholder="例如：核验是否提供有效营业执照，并确认经营范围符合要求"></textarea></label><label>招标原文依据（可选）<textarea id="global-rule-source" rows="4" placeholder="如有固定依据可填写；项目内仍可编辑或删除"></textarea></label><label class="inline-check"><input id="global-rule-ocr" type="checkbox"> 关键证据需要 OCR 识别</label><label class="inline-check"><input id="global-rule-enabled" type="checkbox" checked> 新项目默认选择此规则</label><div class="actions"><button id="save-global-rule" class="primary">保存通用规则</button><button id="reset-global-rule-form">取消编辑</button></div></article>
+                    <article class="model-editor"><h3 id="global-rule-form-title">新增通用规则</h3><input id="global-rule-id" type="hidden"><label>分类<select id="global-rule-category"><option value="qualification">资格性</option><option value="compliance">符合性</option><option value="substantive" selected>实质性/废标项</option><option value="other">其他规则</option></select></label><label>规则名称<input id="global-rule-title" placeholder="例如：营业执照有效性"></label><label>检查规则<textarea id="global-rule-check" rows="4" placeholder="例如：核验是否提供有效营业执照，并确认经营范围符合要求"></textarea></label><label>招标原文依据（可选）<textarea id="global-rule-source" rows="4" placeholder="如有固定依据可填写；项目内仍可编辑或删除"></textarea></label><label>图片取证<select id="global-rule-acquisition"><option value="off" selected>不启用：仅全文文字审查</option><option value="smart">智能取证（推荐）：证据不足时按需启动</option><option value="always">每次执行：由系统选择必要通道</option><option value="custom" disabled>保留专家自定义设置</option></select></label><label id="global-rule-coverage-field">取证强度<select id="global-rule-coverage"><option value="low">快速：单页材料或快速抽查</option><option value="standard" selected>标准（推荐）：常见材料与必要补页</option><option value="high">充分：材料分散、页数较多或风险较高</option></select></label><label class="inline-check"><input id="global-rule-ocr" type="checkbox"> 包含扫描件或图片材料（可选提示）</label><label class="inline-check"><input id="global-rule-enabled" type="checkbox" checked> 新项目默认选择此规则</label><p class="hint">通常选择“智能取证”即可。系统会按规则内容决定 OCR 或图片外观核验；仅当签章、勾选或图片材料必须每次检查时选择“每次执行”。</p><div class="actions"><button id="save-global-rule" class="primary">保存通用规则</button><button id="reset-global-rule-form">取消编辑</button></div></article>
                     <article class="model-list"><div><h3>已配置通用规则</h3><p class="hint">全部规则都会导入今后新建的项目；可在这里设置进入项目后是否默认勾选，项目内仍可按需调整。</p></div><div id="global-rules" class="model-profiles-scroll muted">正在加载…</div></article>
                 </div>
             </section>
